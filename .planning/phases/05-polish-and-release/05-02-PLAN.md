---
phase: 05-polish-and-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: false
requirements:
  - DIST-02

must_haves:
  truths:
    - "A non-technical Obsidian user can read README and know what the plugin does in 2-3 sentences"
    - "A non-technical user can follow the Installation section step-by-step without developer knowledge"
    - "API key configuration appears as a numbered step inside Installation (not a separate section)"
    - "How to use covers all three entry points: command palette, context menu, sidebar"
    - "README contains no sample plugin boilerplate content"
  artifacts:
    - path: "README.md"
      provides: "User-facing plugin documentation"
      contains: "Installation"
  key_links:
    - from: "README.md Installation section"
      to: "Plugin Settings API key field"
      via: "numbered step referencing Plugin Settings"
      pattern: "Plugin Settings"
---

<objective>
Rewrite README.md from scratch for non-technical Obsidian users. Replace the sample plugin boilerplate with user-facing documentation covering what the plugin does, how to install it (including API key setup), and how to use all three entry points.

Purpose: The Obsidian store displays README.md on the plugin detail page. New users read this before installing. The current content is developer-facing sample plugin boilerplate and must be replaced before submission.
Output: A new README.md at the repo root with the locked section structure from CONTEXT.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-polish-and-release/05-CONTEXT.md
@.planning/phases/05-polish-and-release/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md with locked section structure</name>
  <files>README.md</files>
  <action>
    Replace the entire contents of README.md. The current file is the Obsidian sample plugin boilerplate - delete all of it and write from scratch.

    Required section structure (locked from CONTEXT.md):
    1. What it does - 2-3 sentences stating the value. Lead with what the user gets. No developer jargon.
    2. Installation - Numbered steps from zero. API key configuration must appear as a numbered step inside this section (not a standalone section). Target reader has never used a plugin before.
    3. How to use - Covers all three entry points: command palette, context menu, sidebar panel.

    Tone and style rules (locked):
    - Target reader: non-technical Obsidian user - no developer knowledge assumed
    - Text-only for v1 - no screenshots, no GIFs, no badges
    - Use "folder" not "directory", "Obsidian settings" not "configuration", "Plugin Settings" for the plugin-specific settings tab
    - Lead with user benefit, not technical mechanism
    - API key step should read naturally, e.g., "In Plugin Settings, paste your OpenAI API key"
    - No "Please" prefix in instructions

    Suggested installation flow (adapt prose as needed):
    1. Open Obsidian and go to Settings > Community Plugins
    2. Turn off Safe Mode if prompted
    3. Click Browse and search for "AI Active Recall"
    4. Click Install, then Enable
    5. Go to Settings > AI Active Recall
    6. Paste your OpenAI API key (get one at platform.openai.com if you don't have one)

    Suggested "How to use" structure:
    - Command palette: explain Cmd/Ctrl+P, "Generate Self-Test for Current Folder"
    - Context menu: explain right-clicking a folder in the file explorer
    - Sidebar panel: explain "Open Active Recall Panel" command, what the panel shows (Generate/Regenerate buttons)

    The exact prose is Claude's discretion - write it to be clear, friendly, and scannable. Do not pad with filler content.
  </action>
  <verify>
    <automated>cd /Users/agustingabrielli/projects/self-test-obsidian-plugin && node -e "const fs = require('fs'); const readme = fs.readFileSync('README.md', 'utf8'); if (readme.includes('Sample Plugin')) throw new Error('Boilerplate not removed'); if (!readme.includes('Installation')) throw new Error('Missing Installation section'); if (!readme.includes('Plugin Settings')) throw new Error('Missing Plugin Settings reference'); if (!readme.includes('command palette') || !readme.includes('context menu') || (!readme.includes('sidebar') && !readme.includes('Sidebar'))) throw new Error('Missing entry point coverage'); console.log('README structure OK')"</automated>
  </verify>
  <done>README.md contains no boilerplate content. Sections: what it does, Installation (with API key as a numbered step), How to use (all 3 entry points). No developer jargon. No screenshots.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify README is suitable for non-technical users</name>
  <action>Read README.md as a non-technical Obsidian user and verify the locked section structure and tone requirements are met.</action>
  <what-built>README.md rewritten for non-technical Obsidian users with the locked section structure: what it does, Installation (API key as a numbered step), How to use (command palette, context menu, sidebar).</what-built>
  <how-to-verify>
    1. Open README.md at the repo root
    2. Read it as a non-technical Obsidian user who has never installed a community plugin
    3. Check: Could you understand what the plugin does in the first paragraph?
    4. Check: Could you follow the Installation steps without developer knowledge?
    5. Check: Is the API key step clearly part of Installation (not buried or in a separate section)?
    6. Check: Do all three entry points (command palette, context menu, sidebar) appear in How to use?
    7. Check: No sample plugin boilerplate, no screenshots placeholder text, no developer-facing content
  </how-to-verify>
  <verify>Manual - see how-to-verify above</verify>
  <done>Human reviewer approves the prose as suitable for a non-technical Obsidian user. All 7 checks pass.</done>
  <resume-signal>Type "approved" if the README reads well for non-technical users, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- README.md contains no boilerplate from sample plugin
- Installation section has API key setup as a numbered step
- How to use covers command palette, context menu, and sidebar
- Human reviewer approves the prose as suitable for a non-technical Obsidian user
</verification>

<success_criteria>
- README.md fully replaced - no sample plugin content
- All three locked sections present with correct content
- Human verification passed ("approved")
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-release/05-02-SUMMARY.md`
</output>
